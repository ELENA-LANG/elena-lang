import extensions;

//function(acc)
//    = (n => acc.append:n);

//accumulator(n)
//    = function(new Variable(n));

A
{
   alt()
   {
      console.writeLine("alt")
   }
}

public program()
{
   var a := new A();
   a.test() \ alt();

//      var n := cast byte(o) ?? nil;
//      (nil == n)
//          ? { ^ false }
//          : { ^ self.equal(n) }


//   console.writeLine(n)

//    var x := accumulator(1);
    
//    x(5);
    
//    var y := accumulator(3);
    
//    console.write(x(2.3r))
}

