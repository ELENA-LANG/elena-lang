import extensions;

public program()
{
  // var text := new extensions'text'StringWriter();
   until(string line := console.readLine(), line.isEmpty())
   {
      console.printLine("test")

    //  text.writeLine(line)
   };   
}