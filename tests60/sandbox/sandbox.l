import extensions;

//function(acc)
//    = (n => acc.append:n);

//accumulator(n)
//    = function(new Variable(n));

A
{
   bool equal(o)
   {
      var n := cast byte(o) ?? nil;
//      (nil == n)
//          ? { ^ false }
//          : { ^ self.equal(n) }
   }
}

public program()
{

//    var x := accumulator(1);
    
//    x(5);
    
//    var y := accumulator(3);
    
//    console.write(x(2.3r))
}

