import system;

A;

singleton Tester
{
   run()
   {
      var message := mssgconst writeLine[1];
      message(console,"Hello");

//      var a := new A();

//      a.fail() \\ on((e){ console.writeLine(e) })
   }
}

public program() 
{
      var message := mssgconst writeLine[1];
      message(console,"Hello");

//   Tester.run();
}
