import extensions;
import extensions'text;

// --- Program ---

public program()
{
    var s := "ab".insert(0, "_");
    
//    var text := /*console.print:"Enter the text:".loadLineTo(new StringWriter())*/"ss";
//    var searchText := "s";  
//    var replaceText := "S";
//
//    var bm := new StringBookmark(text);
//
//    while (bm.find(searchText))
//    {
//        bm.delete(searchText).insert(replaceText)
//    };
//
//    console
//        .printLine("The resulting text:",bm)
//        .readChar() // wait for any key
}
