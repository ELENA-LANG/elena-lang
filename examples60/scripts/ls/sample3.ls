import extensions;
import extensions'text;

// --- Program ---

public Program()
{
  var n := 2;
  
  if (n == 2)
  {
      console.printLine("n==2")
  }
  else
  {
     console.printLine("n!=2")
  }
}
