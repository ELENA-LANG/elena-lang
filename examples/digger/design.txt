game object 

  2d coordinate
  2d vector   (so for moving object, it should provide enough offset to compensate gravitation)

  type : fixed : / collectable (e.g. stone is not fixed but not collectable, diamon should contain
                                some value)
          killable (e.g with another object: e.g. monster or stone)
          killer   (e.g. monster or stone)
  
